"""AI-фильтр релевантности по смыслу + категория + флаг 'бесплатно'.

Веха 2. Двухступенчатый:
  1) дешёвый предфильтр (ключевые слова + актуальность даты) — отсев мусора/старья;
  2) Claude API — только то, что прошло шаг 1 (смысл + раскладка по категориям).
Так экономим API при сохранении точности.
"""
